<?php

namespace App\Controllers;

use App\Libraries\Form;
use App\Libraries\Utils;
use App\Model;
use CodeIgniter\API\ResponseTrait;

class HerbController extends BaseController
{
    use ResponseTrait;


    public function __construct ()
    {
        $util = New Utils;
        $Auth = [AUTH_MASTER];
        $util->fn_Check_Auth($Auth);
    }

    public function herbList(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else {
            $request = service('request');
            $skey = ($request->getPost('skey')=='') ? '' : $request->getPost('skey');
            $page = ($request->getPost('page')=='') ? 1 : $request->getPost('page');
            $limit = 50;

            $s_data = [];

            $metaarr = [
                'h_title' => '상품리스트',
                'h_type' => 1
            ];

            $body = [
                'skey' => $skey,
                'page' => $page,
                'limit' => $limit
            ];

            $form = new Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr),
                'search' => $form->fnMake_Search($s_data),
                'body' => $body
            ];

            return view('web/master/herbListMaster_View', $main_data);
        }
    }

    public function herbMatch(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else {
            $request = service('request');
            $cf_code = ($request->getPost('cf')=='') ? '' : $request->getPost('cf');
            $hn_name = ($request->getPost('hn')=='') ? '' : $request->getPost('hn');
            $page = ($request->getPost('page')=='') ? 1 : $request->getPost('page');
            $limit = 50;

            $s_data = [];

            $metaarr = [
                'h_title' => '약재매칭',
                'h_type' => 1
            ];

            $decocarr = $util->fn_LoadDecoc(AUTH_DECOC);
            $decocoption = '';
            if($util->fnArrayCnt($decocarr)>0){
                foreach ($decocarr as $c){
                    if($cf_code==$c['mi_cfcode']){
                        $decocoption .= "<option selected value='{$c['mi_cfcode']}'>{$c['mi_name']}</option>";
                    }else{
                        $decocoption .= "<option value='{$c['mi_cfcode']}'>{$c['mi_name']}</option>";
                    }
                }
            }

            $body = [
                'cf_code' => $cf_code,
                'hn_name' => $hn_name,
                'option' => $decocoption,
                'page' => $page,
                'limit' => $limit
            ];

            $form = new Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr),
                'search' => $form->fnMake_Search($s_data),
                'body' => $body
            ];

            return view('web/master/herbMatch_View', $main_data);
        }
    }

    public function updateApproval() {
        $authentifier = $this->request->getPost('authentifier');
        $model = new Herb_m();
        $model->updateHnIsok($authentifier, 1); // 1로 업데이트
        return $this->respond(['status' => 'success']);
    }


    public function orderList(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $uid = $sessinarr['user']['uid'];
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '전체주문내역',
                'h_type' => 1
            ];
            $request = service('request');
            $micode = ($request->getPost('micode')=='') ? '' : $request->getPost('micode');
            $wocode = ($request->getPost('wocode')=='') ? '' : $request->getPost('wocode');


            $decocarr = $util->fn_LoadDecoc(AUTH_DECOC);
            $decocoption = '';
            if($util->fnArrayCnt($decocarr)>0){
                foreach ($decocarr as $c){
                    if($micode==$c['mi_code']){
                        $decocoption .= "<option selected value='{$c['mi_code']}'>{$c['mi_name']}</option>";
                    }else{
                        $decocoption .= "<option value='{$c['mi_code']}'>{$c['mi_name']}</option>";
                    }
                }
            }

            $pharmarr = $util->fn_LoadDecoc(AUTH_PHARM);
            $pharmoption = '';
            if($util->fnArrayCnt($pharmarr)>0){
                foreach ($pharmarr as $c){
                    if($wocode==$c['mi_code']){
                        $pharmoption .= "<option selected value='{$c['mi_code']}'>{$c['mi_name']}</option>";
                    }else{
                        $pharmoption .= "<option value='{$c['mi_code']}'>{$c['mi_name']}</option>";
                    }
                }
            }

            $body_data = [
                'uid' => $uid,
                'option1' => $decocoption,
                'option2' => $pharmoption,
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr),
                'body' => $body_data
            ];

            return view('web/master/orderListMaster_View', $main_data);
        }
    }
    public function deliveryList(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '상품등록',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/master/deliveryListMaster_View', $main_data);
        }
    }

    public function claim(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '상품등록',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/master/claim_View', $main_data);
        }
    }


    public function claimList(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '취소·반품·교환 리스트',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/master/claimList_View', $main_data);
        }
    }


    public function reOrder(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '상품등록',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/master/reOrder_View', $main_data);
        }
    }


    public function burkOrderForm(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $uid = $sessinarr['user']['uid'];
            $mi_type = $sessinarr['user']['mi_type'];

            $metaarr = [
                'h_title' => '대량주문 양식',
                'h_type' => 1
            ];

            if ($mi_type != 'master') {
                $result = 'type101';
                $message = '권한이외의 접근입니다.';
            } else {
                $herb_m = model('Herb_m');
                $decocarr = $util->fn_LoadDecoc('decoc');
                $decocoption = '';
                if($util->fnArrayCnt($decocarr)>0){
                    $decocoption = '<option selected value="0">선택하세요.</option>';
                    foreach ($decocarr as $c){
                        $decocoption .= "<option value='{$c['mi_code']}'>{$c['mi_name']}</option>";
                    }
                }else{
                    $decocoption = "<option value=''>옵션 없음.</option>";
                }

                $body_data = [
                    'uid' => $uid,
                    'd_list' => $decocoption
                ];

            }

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr),
                'body' =>$body_data
            ];

            return view('web/master/burkOrderForm_View', $main_data);
        }
    }

    public function popThum(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '상품등록',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/include/pop_ThumMaster_View', $main_data);
        }
    }


    public function popReject(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $mi_type = $sessinarr['user']['mi_type'];
            $metaarr = [
                'h_title' => '상품등록',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/include/pop_RejectMaster_View', $main_data);
        }
    }



    public function dashBoard(){
        $util = New Utils;
        $sessinarr = $util->fnGetSessionData();
        if(!$sessinarr['islogin']){
            return redirect()->to('/Member/Login')->with('msg','로그인이 필요합니다.');
        }else{
            $metaarr = [
                'h_title' => '상품등록',
                'h_type' => 1
            ];

            $form = New Form;
            $main_data = [
                'meta' => $form->fnMake_Meta($metaarr),
                'header' => $form->fnMake_Header($sessinarr),
                'left_menu' => $form->fnMake_Left($sessinarr)
            ];

            return view('web/master/dashBoard_View', $main_data);
        }
    }

}
