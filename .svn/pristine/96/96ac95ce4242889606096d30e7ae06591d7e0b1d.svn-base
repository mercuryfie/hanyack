$(document).ready(function() {
    Load_Herb(1, '');

    $('#XBtn,#cnxl').on('click',function(e){
        // INI_Matching_pop();
        $('#eventPop').hide();
    });


    $('#eventPop').on('click', function(e){
        if (e.target === this) {
            // INI_Matching_pop();
            $(this).hide();
        }
    });


    //popReject 시 버튼
    $('#btn_reject').on('click',function(e){
        let hncode = $('#btn_reject').data('hncode');
        let hntype = $('#reject_issue option:selected').val();
        let hnmemo = $('#reject_memo').val();
        if((hncode=='') ||(hntype=='') ||(hnmemo=='')){
            alert('반려 사유를 확인하세요.');
        }else {
            let dataarr = {'hncode':hncode,'hntype':hntype,'hnmemo':hnmemo}
            Reject_Do(dataarr);
        }
    });

    $('#btn_close').on('click',function(e){
        Ini_popReject();
        $('#itemRejectpop').hide();
    });

    $('#closeThum').on('click',function(e){
        Ini_popThum();
        $('#thumPopCon').remove();
    });


    $('#thumPopCon').on('click', function(e) {
        if (e.target === this) {
            $(this).remove();
        }
    });

    $('#itemRejectpop').on('click', function(e) {
        if (e.target === this) {
            $(this).remove();
        }
    });


    // about popEvent
    $('#btn_cancle').on('click',function(e){
        Ini_popEvent();
        location.reload();
    });

    $(document).on('click', '[data-remove="removeBtn"]', function() {
        Ini_popEvent();
        $(this).closest('[data-add="addEvent"]').remove();
    });

    $('#submitBtn').on('click',function () {

        let hncode = '';
        let dataTypeArr = [];
        $('#eventTable').find('tr[data-add="addEvent"]').each(function() {
            hncode = ($(this).find('[data-hncode]').data('hncode'));
            // dataTypeArr.push($(this).find('[data-type]').data('data-type'));
            dataTypeArr.push($(this).find('[data-type]').attr('data-type'));
        });
        let dataarr = {
            "hncode":hncode,
            "dataType":dataTypeArr,
        }
        console.log('this1029',dataarr);
        console.log('this1029',dataTypeArr);

        if (dataTypeArr.length <0) {
            Make_Toast('타입을 선택하십시오.');
        } else {
            submit_EventType(dataarr);

        }
    });

    // $('#submitBtn').on('click',function () {
    //
    //     let hncodeArr = [];
    //     let dataTypeArr = [];
    //     $('#eventTable').find('tr[data-add="addEvent"]').each(function() {
    //         hncodeArr.push($(this).find('[data-hncode]').data('hncode'));
    //         dataTypeArr.push($(this).find('[data-type]').attr('data-type'));
    //     });
    //     // $('#eventTable').find('tr[data-add="addEvent"]').each(function() {
    //     //     let hncode = $(this).find('[data-hncode]').data('hncode');
    //     //     let dataType = $(this).find('[data-type]').attr('data-type');
    //     //     dataarr.push({ 'hncode': hncode, 'dataType': dataType });
    //     // });
    //     let dataarr = {
    //         "hncode":hncodeArr,
    //         "dataType":dataTypeArr,
    //     }
    //     console.log('this1707',dataarr);
    //
    //     let typeArr = [];
    //
    //     $('#eventTable').find('[data-type]').each(function() {
    //         typeArr.push($(this).attr('data-type'));
    //     });
    //     //
    //     // let dataarr = {
    //     //     "hncode":$('#hncode').text(),
    //     //     "hnname": $('#hnname').text(),
    //     //     "typeArr":typeArr
    //     // };
    //     // console.log(typeArr);
    //     // console.log(dataarr);
    //     if (typeArr == '') {
    //         Make_Toast('타입을 선택하십시오.');
    //     } else {
    //         submit_EventType(dataarr);
    //
    //     }
    // });


    $('#yakApprove').on('click',function(e){
        let checked = $('input[name=okCheck]:checked');
        let cnt = checked.length;
        let hncode = $('input[name=okCheck]:checked').map(function() {
            return $(this).data('id');
        }).get();
        let hnisok = $('input[name=okCheck]:checked').data('status');
        // alert(hncode);
        if (cnt <=0){
            alert('승인할 약재를 선택하세요.');
        } else {
            let imi = checked.filter(function() {
                return $(this).data('status') == 100;
            }).length > 0;
            if (imi) {
                alert('이미 승인한 약재가 포함되어 있습니다.');

            } else{
                if(window.confirm("승인하시겠습니까?")==true) {

                    let restr = '';
                    checked.each(function() {
                        if(restr==''){
                            restr = $(this).data('id');
                        }else{
                            restr += ',' + $(this).data('id');
                        }
                    });

                    let dataarr = {"hncode" : restr,"hntype" : 100};
                    Approval_Do(dataarr);
                }
            }
        }
    });

    //product_cancel 처리 버튼

    $('#yakCancel').on('click',function(e){
        let checked = $('input[name=okCheck]:checked');
        let cnt = checked.length;
        let hncode = $('input[name=okCheck]:checked').map(function() {
            return $(this).data('id');
        }).get();
        let hnisok = $('input[name=okCheck]:checked').data('status');
        // alert(hncode);
        if (cnt <=0){
            alert('승인 취소할 약재를 선택하세요.');
        } else {
            let imi = checked.filter(function() {
                return $(this).data('status') == 0;
            }).length > 0;
            if (imi) {
                alert('이미 미승인 상태인 약재가 포함되어 있습니다.');

            } else{
                if(window.confirm("승인 취소하시겠습니까?")==true) {

                    let restr = '';
                    checked.each(function() {
                        if(restr==''){
                            restr = $(this).data('id');
                        }else{
                            restr += ',' + $(this).data('id');
                        }
                    });

                    let dataarr = {"hncode" : restr,"hntype" : 0};
                    Cancel_Product_Do(dataarr);
                }
            }
        }
    });

    $('#yakReject').on('click',function(e){
        let cnt = $('input[name=okCheck]:checked').length;
        if (cnt <=0){
            alert('취소하실 약재를 선택하세요.');
        } else if(cnt > 1){
            alert('반려 처리는 한개씩 가능합니다.');
        } else{
            Ini_popReject();

            let hncode = $('input[name=okCheck]:checked').data('id');
            let isok = $('input[name=okCheck]:checked').data('status');
            if(isok==1){
                alert('이미 반려 상태 입니다.');
            }else{
                $('#btn_reject').data('hncode',hncode);
                $('#itemRejectpop').show();
            }
        }
    });


    $("#more,#more2").on("click", function (key) {
        let page = $('#more').data('page');
        Load_Herb(page,'');
    });

});

function submit_EventType2(dataarr) {
    console.log('this',dataarr);
}

async function submit_EventType(dataarr) {
    console.log('this111',dataarr);
    try {
        start_spinner();
        let url = APIURL + '/Insert_ProductEvent';
        let result = await Load_API2(url,dataarr);
        if(result.get('status')=='NoLogin'){
            go_login();
        }else if(result.get('status')=='ok'){
            location.reload();
            Make_Toast('이벤트 타입을 등록했습니다.');
        }else{
            Make_Toast('2이벤트 타입을 등록했습니다.');
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        Make_Toast('3이벤트 타입을 등록했습니다.');
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}


async function Reject_Do(dataarr) {
    try {
        start_spinner();
        let url = APIURL + '/Update_Product_Reject';
        let result = await Load_API(url,dataarr);
        if(result.get('status')=='NoLogin'){
            go_login();
        }else if(result.get('status')=='ok'){
            location.reload();
            alert('해당 약재를 반려했습니다.');
        }else{
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

async function Approval_Do(dataarr) {
    try {
        start_spinner();
        let url = APIURL + '/Update_Product_IsOk';
        let result = await Load_API(url,dataarr);
        if (result.get('status') == 'NoLogin') {
            go_login();
        }else if(result.get('status') == 'ok') {
            alert('해당 약재를 승인했습니다.');
            location.reload();
        }else{
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

async function Cancel_Product_Do(dataarr) {
    try {
        start_spinner();
        let url = APIURL + '/Update_Product_IsOk';
        let result = await Load_API(url, dataarr);
        if (result.get('status') == 'NoLogin') {
            go_login();
        } else if (result.get('status') == 'ok') {
            alert('해당 약재를 승인 취소 하였습니다.');
            location.reload();
        } else {
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

function Ini_popReject(){
    $('#reject_issue').val(0);
    $('#reject_memo').val('');
    $('#btn_reject').data('hncode','');
}

function Ini_popThum(){
    $('#thumPopCon').data('hncode','');
}

function Ini_popEvent(){
    $('[data-hncode]').data('hncode','');
    $('[data-type]').data('type','');
    $('#thumPopCon').data('hncode','');
    $('#miname').val('');
    $('#hncode').val('');
    $('#hnname').val('');
}


function INI_Form(){
    $('#herbtable').empty();
}

function imageload(fname){
    let url = '/assets/product/image/' + fname;
    $('#hn_thum').attr('src',url);
    // $('#hn_thum').src = url;
    $('#thumPopCon').show();
}

function pdfload(fname){
    let url = '/assets/product/data/' + fname;
    window.open(url);
}

function go_itemDetail(hn_code) {
    let url = '/Product/itemDetail/' + hn_code;
    window.open(url, '_blank');
}

function setEvent2(hncode,miname,hnname) {
    console.log(hncode,miname,hnname);
}

async function setEvent3(dataarr) {
    try {
        start_spinner();
        let url = APIURL + '/Load_herbList';
        let result = await Load_API(url, dataarr);
        if (result.get('status') == 'NoLogin') {
            go_login();
        } else if (result.get('status') == 'ok') {
            let data = result.get('data');
            let arr = (data && data.list) ? data.list : [];
            let Cnt = arr.length;
            if (Cnt > 0) {
                $.each(arr, function (index, el) {
                    console.log(el.hn_isok);
                });
            }
        } else {
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

async function add_NewEvent() {

    try {
        let result = await add_NewEvent_Data();
        if(result.status == 'ok') {
            Make_Toast('저장하였습니다.');
            do_refresh();
            console.log('bello1');
        }

    } catch (error) {
        Make_Toast('bello.');
        console.log('bello333333');
        console.log( error );
    }
}

function add_NewEvent_Data(){
    return new Promise(function(resolve, reject) {

        const $root = $(`[name="newAddress"]`);
        console.log('$container:', $root.length);
        console.log('$container:', mizip,add1);
        let formdata = new FormData($("#thisForm")[0]);
        let add2    = $root.find('[name="Add2"]').val();
        let miname  = $root.find('[name="miname"]').val();
        let ctc     = $root.find('[name="ctc"]').val();
        let isDefault = $root.find('[name="isDefault"]').is(':checked') ? 1 : 0;

        formdata.append("zonecode", mizip);
        formdata.append("Add1", add1);
        formdata.append("Add2", add2);
        formdata.append("miname", miname);
        formdata.append("ctc", ctc);
        formdata.append("isDefault", isDefault);

        let retarr = new Array();
        for (let pair of formdata.entries()) {
            console.log('bello 13',pair[0]+ ': ' + pair[1]);
        }

        $.ajax({
            url: APIURL + '/Insert_DeliInfo',
            type : 'POST',
            data: formdata,
            dataType: "JSON",
            cache : false,
            // enctype		: 'multipart/form-data',
            processData : false,
            contentType : false,

            success : function(response) {
                let retarr = {};
                if(response.result=='ok'){
                    retarr['status'] = 'ok';
                    retarr['code'] = response.code;
                    retarr['message'] = 'success';
                    resolve(retarr);
                }else{
                    retarr['status'] = 'error';
                    retarr['code'] = response.code;
                    retarr['message'] = response.message;
                    reject(retarr);
                }
            },
            error: function(error) {
                retarr['code'] = 'ConnERR';
                retarr['message'] = error;
                reject(retarr);
            }
        });
    });
}

async function Load_ProductEvent(hncode) {
    console.log('this111',dataarr);
    try {
        start_spinner();
        let url = APIURL + '/Load_ProductEvent';
        let result = await Load_API(url,dataarr);
        if(result.get('status')=='NoLogin'){
            go_login();
        }else if(result.get('status')=='ok'){
            addEvent(hncode);
            console.log(result.get('data'));

        }else{
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        Make_Toast('3로드 실패.');
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

function setEvent(miname,hncode, hnname){
    Ini_popEvent();
    $('#miname').text(miname);
    $('#hncode').text(hncode);
    $('#hnname').text(hnname);

    $('#hnname').text(hnname);
    $('#hnname').text(hnname);

    Load_ProductEvent(hncode);
    $('#eventPop').show();
}

async function Load_ProductEvent(hncode) {
    console.log('this111',dataarr);
    try {
        start_spinner();
        let url = APIURL + '/Load_ProductEvent';
        let result = await Load_API(url,dataarr);
        if(result.get('status')=='NoLogin'){
            go_login();
        }else if(result.get('status')=='ok'){
            addEvent(hncode);
            console.log(result.get('data'));

        }else{
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        Make_Toast('3로드 실패.');
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

function addEvent(hncode) {
    console.log('bello',hncode);
    let value = $('#eventType').val();
    let hnname = $('#hnname').text();
    console.log('hello15',value);

    if ($('[data-type="' + value + '"]').length > 0) {
        Make_Toast('이미 등록된 타입입니다.');

    } else {
        if (value == 1) {
            let html = '';
            html += ` 
            <tr data-add="addEvent" class="addEvent">
                <td class="hncode" data-hncode="`+hncode+`">`+hncode+`</td>
                <td class="hnname" >`+hnname+`</td>
                <td class="" data-type="1">인기 한약재</td>
                <td class="delete">
                    <i class="fa-solid fa-xmark " id="" data-remove="removeBtn"></i>
                </td> 
            </tr>
        `;
            $('#eventTable').append(html);

        } else if (value ==2 ) {
            let html = '';
            html += ` 
                <tr data-add="addEvent" class="addEvent"> 
                    <td class="hncode" data-hncode="`+hncode+`">`+hncode+`</td>
                    <td class="hnname">`+hnname+`</td>
                    <td class="" data-type="2">추천 한약재</td>
                    <td class="delete">
                        <i class="fa-solid fa-xmark " id="" data-remove="removeBtn"></i>
                    </td> 
                </tr>
        `;
            $('#eventTable').append(html);

        } else {
            let html = '';
            html += ` 
                <tr data-add="addEvent" class="addEvent">
                    <td class="hncode" data-hncode="`+hncode+`">`+hncode+`</td>
                    <td class="hnname">`+hnname+`</td>
                    <td class="" data-type="3">신규 한약재</td>
                    <td class="delete">
                        <i class="fa-solid fa-xmark " id="" data-remove="removeBtn"></i>
                    </td> 
                </tr>
        `;
            $('#eventTable').append(html);
        }
    }
}

async function Load_Herb(page, skey) {
    try {
        start_spinner();
        let dataarr = {"page": page, "skey": skey,"typ" : 100};
        let url = APIURL + '/Load_herbList';
        let result = await Load_API(url,dataarr);
        console.log(dataarr);
        if (result.get('status') == 'NoLogin') {
            go_login();
        } else if(result.get('status') == 'ok') {
            let html = '';
            let firstsn = '';
            let data = result.get('data');
            let arr = (data && data.list) ? data.list : [];
            let Cnt = arr.length;
            if (Cnt > 0) {
                $.each(arr, function (index, el) {
                    let authStatus_str = '';
                    let subimg = '';
                    let subfile = '';

                    if(firstsn=='' && page > 1){
                        firstsn = el.sn;
                    }

                    if(el.hn_isok==100){
                        authStatus_str = 'style="color:green;"';
                    }else if(el.hn_isok==0){
                        authStatus_str = 'style="color:red;"';
                    }else if(el.hn_isok==1){
                        authStatus_str = 'style="color:#5c5c5c;"';
                    }

                    if(el.fname.img!='') {
                        subimg = `<i class="fa-regular fa-image" id="prevThum" name="prevThum" onclick="imageload('${el.fname.img}');"></i>`;
                    }
                    if(el.fname.data!='') {
                        subfile = `<i class="fa-regular fa-file" id="prevPDF" name="prevPDF" onclick="pdfload('${el.fname.data}');"></i>`;
                    }
                    let ctrBtn =`<button type="button" 
                        onclick="setEvent('${el.mi_name}','${el.hn_code}','${el.hn_name}');" 
                        class="btnType1" name="ctrBTn">설정</button>`;

                    html += `
                        <tr id="line_${el.sn}">
                            <td class="giveDataid"><input type="checkbox" name="okCheck" class="approvalCheckbox column-1" data-id="${el.hn_code}" data-status ="${el.hn_isok}"></td>
                            <td class="authStatus" ${authStatus_str}>${el.hn_isok_str}</td>
                            <td>${el.hn_code}</td>
                            <td>${el.mi_name}</td>
                            <td>${el.hn_name}</td>
                            <td>${el.fk_mdname}</td>
                            <td>${el.n_value}</td>
                            <td>${el.t1_value}</td>
                            <td>${el.t2_value}</td>
                            <td>${el.w_name}</td>
                            <td>${number_format(el.hn_gPrice)}원</td>
                            <td>${number_format(el.hn_pPrice)}원</td>
                            <td>${number_format(el.hn_stock)}개</td>
                            <td>${ctrBtn}</td>
                            <td class="preview">
                                ${subimg}
                                ${subfile}
                                <i class="fa-solid fa-circle-info" onclick="go_itemDetail('${el.hn_code}');"></i>
                            </td>
                        </tr>    
                    `;

                });

                $('#more').data('page', result.get('data').page);
                $('#herbList').append(html);
                if(firstsn!=''){
                    let $targetTr = $('#line_' + firstsn); // 예: 6번째 tr
                    $targetTr.attr("tabindex", -1);
                    $('html, body').animate({scrollTop: $targetTr.offset().top}, 400);
                    firstsn = '';
                }
            }else{
                alert('마지막 입니다.');
                $('.moreListBox').css('display','none');
            }

        }else{
            alert(result.get('message'));
        }
        stop_spinner();
    } catch (error) {
        alert('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}




