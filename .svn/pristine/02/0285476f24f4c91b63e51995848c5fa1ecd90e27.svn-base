$(document).ready(function(){
    let page = $('#more1').data('page');
    let skey = $('#hnname').val();
    let stype = $('#stype').val();
    let snation = $('#snation').val();

    Load_Stock(page,skey,stype,snation);

    $(document).on('click','#more1,#more2',function(){
        let page = $('#more1').data('page');
        let skey = $('#hnname').val();
        let stype = $('#stype').val();
        let snation = $('#snation').val();

        Load_Stock(page,skey,stype,snation);
    });

    $(document).on('keydown', '#hnname', function(e) {
        if (e.key === 'Enter' || e.keyCode === 13) {
            Form_ini();
            let page = $('#more1').data('page');
            let skey = $('#hnname').val();
            let stype = $('#stype').val();
            let snation = $('#snation').val();
            Load_Stock(page,skey,stype,snation);
        }
    });

    $(document).on('change','#stype,#snation',function(){
        Form_ini();
        let page = $('#more1').data('page');
        let skey = $('#hnname').val();
        let stype = $('#stype').val();
        let snation = $('#snation').val();
        Load_Stock(page,skey,stype,snation);
    });

});


async function Load_Stock(page,skey,stype,snation){
    try {
        start_spinner();
        let dataarr = {"page": page, "skey": skey,"stype" : stype,"snation":snation};
        let url = APIURL + '/Load_DecocTotalStock';
        let result = await Load_API(url, dataarr);
        if (result.get('status') == 'NoLogin') {
            go_login();
        } else if (result.get('status') == 'ok') {
            let html = '';
            let data = result.get('data');
            let arr = (data && data.list) ? data.list : [];
            let Cnt = arr.length;
            let stylestr = '';
            if (Cnt > 0) {
                $.each(arr, function (index, el) {
                    if(el.stock==1){
                        stylestr = '';
                    }else{
                        stylestr = `style="background-color: #f8d7da61;"`;
                    }

                    html += `
                        <tr ${stylestr}>
                            <td>${el.mmCnt}</td>
                            <td>${el.mmMedicine}</td>
                            <td>${el.mmTitle}</td>
                            <td>${number_format(el.mmCapa)}g</td>
                            <td>${number_format(el.mmStable)}g</td>
                            <td>${number_format(el.mmdelCnt)}건</td>
                        </tr>
                    `;
                });
            }else{
                if(page==1) {
                    html = '<tr><td colspan="6">재고 정보가 없습니다.</td></tr>'
                }else if(skey!='') {
                    Make_Toast('검색하신 약재는 존재하지 않는 약재입니다.');
                }else{
                    Make_Toast('마지막입니다.');
                }
            }
            $('#stocklist').append(html);
            $('#more1').data('page',data.page);
        }else{
            Make_Toast(result.get('message'));
        }
        stop_spinner();
    }catch(error){
        Make_Toast('오류가 발생하였습니다. 다시 시도하여주세요.\n[ERROR : ' + error + '}');
        stop_spinner();
    }
}

function Form_ini(){
    $('#stocklist').empty();
    $('#more1').data('page',1);
}